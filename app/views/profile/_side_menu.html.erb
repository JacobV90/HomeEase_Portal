<nav class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <div class="brand-wrapper">
            <!-- Hamburger -->
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".side-navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <!-- Brand -->
            <div class="brand-name-wrapper">
                <a class="navbar-brand" href="#" style = " font-size: 140%;">
                    Home Ease Management
                    
                </a>
            </div>

        </div>

    </div>

    <!-- Main Menu -->
    <div class="side-menu-container side-navbar-collapse">
        <ul class="nav navbar-nav">

            <li>
              <%= link_to dashboard_path do %> 
                   <%= content_tag :span, "", :class => "glyphicon glyphicon-dashboard" %>
                    Dashboard
               <% end %>
            </li>
            <!-- Dropdown-->
            <li class="panel panel-default" id="dropdown">
                <a data-toggle="collapse" href="#dropdown-lvl1">
                        <span class="glyphicon glyphicon-home"></span>Properties<span class="caret"></span>
                </a>

                <!-- Dropdown level 1 -->
                <div id="dropdown-lvl1" class="panel-collapse collapse">
                    <div class="panel-body">
                        <ul class="nav navbar-nav" id="property-dropdown">
                             <li>
                                 <%= link_to new_property_path do %> 
                                     <%= content_tag :span, "", :class => "glyphicon glyphicon-plus" %>
                                     Add a property
                                 <% end %>                             
                             </li>
                            <% (Property.where("user_id = '#{current_user.id}'")).each do |prop| %>
                                <li>
                                    <%= link_to prop.street.to_s, property_path(:id => prop.id) %>
                                </li>
                            <% end %>
                        </ul>
                    </div>
                </div>
            </li>
            
            <li>
                <%= link_to maintenanceIssue_path do %> 
                   <%= content_tag :span, "", :class => "glyphicon glyphicon-wrench" %>
                    Repairs
                <% end %>
            </li>
            
            <li>
               <%= link_to bank_path do %> 
                   <%= content_tag :span, "", :class => "glyphicon glyphicon-credit-card" %>
                    Bank
               <% end %>
            </li>

            
            <li>
                <%= link_to settings_path do %> 
                   <%= content_tag :span, "", :class => "glyphicon glyphicon-cog" %>
                    Settings
                <% end %>
            </li>

        </ul>
    </div><!-- /.navbar-collapse -->
</nav>